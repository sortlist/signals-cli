{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v signals >/dev/null 2>&1 || (echo 'Signals CLI not found. Install with: npm install -g signals-sortlist-cli' >&2 && exit 0)"
          }
        ]
      }
    ]
  }
}
